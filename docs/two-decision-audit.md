# Prompt C checkout audit — 20 September 2026

The remaining prompt in the downloaded professional plan is C. A, B and their TUI follow-up are already implemented in this dirty checkout; preserve them. No applicable AGENTS.md exists in the repository or ancestors. Root build/lock configuration is unchanged.

Baseline: `.venv/bin/pytest -q engine/tests/test_decision_verification.py engine/tests/test_funding_cuts.py engine/tests/test_studio.py engine/tests/test_precision_b.py` — **68 passed**, two existing dependency deprecations, 10.86 s; raw output in `artifacts/two-decision/baseline-tests.txt`.

Existing code to reuse: `execution.snapshot` provides immutable owned arrays; `risk.probabilities` and `risk.cvar` define weighted/tied empirical conventions; `sampling.derive_seed` and `decision_lab.independent_streams` supply ordinary-MC provenance checks. CLI and `studio.py` already route bounded offline experiments and display structured results/tables. The existing funding optimizer, settlement helper, account quotes, verifier and decision artifacts are single-decision contracts; their tests are implementation evidence, but they are not a scenario-tree policy implementation.

No executable two-decision/nonanticipativity implementation was found. `docs/engine-review-2026-09-13.md` proposes one; that proposal is not shipped code. Prompt C requires a separate model version. In particular, production `settlement_forecast_day` is one-indexed with a minimum day-one availability convention; the experiment will explicitly use execution times 0 and review-day start, with positive calendar-day settlement offsets. It will not silently change production dates or repurpose `PlanSpec`.

Bounded implementation: synthetic four-leaf joint cash/price law, two predefined review-state features, one asset, one capped credit facility, discrete actions, expected terminal-wealth loss plus priced negative-cash dollar-days. Enumerate the tiny action grid, using conditional separability for the expectation objective; independently enumerate the full joint policy product in tests. CVaR remains a descriptive deficit metric, not a dynamic risk objective. Compare static, nonanticipative and common-root hindsight-relaxed policies on exactly the same finite supplied futures. Freeze training policies before drawing an independent validation ensemble; no holdout selection. Preserve exact synthetic support and draws for offline replay.

Completed evidence: `docs/two-decision.md` describes the final separate model and its consumers. Full-suite validation passed 559 tests/37 snapshots; the final focused suite has 17 passing tests. The synthetic capture replays without mismatches. Five retained measurement cases include no benefit from review, lower objective with cash-policy failure, late settlement, action instability and a separately labeled offline stress draw count. During development, replay caught tuple/NumPy scalar versus JSON scalar representation differences in newly added observation records; explicit primitive serialization fixed those differences without weakening numerical tolerances. The independent arithmetic comparisons remained passing. No production funding optimizer, native kernel, web consumer or persistence schema was rewritten.
